name: Agentic Approval
# DISABLED: This workflow polled for up to 30 minutes (90 attempts x 20s)
# waiting for other checks, burning Actions minutes on every PR.
# Re-enable when needed with a shorter timeout.

on:
  workflow_dispatch: {}

jobs:
  gate:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
